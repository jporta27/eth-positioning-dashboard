"""ETH positioning dashboard — event study framework.

Answers "does this signal have edge?" defensibly and reproducibly for any
column in the parquet layout (backfill/ + data/YYYY-MM-DD/).

Entry point: python -m backtest.cli --help
"""
__version__ = "0.1.0"
